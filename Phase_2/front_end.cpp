#include <fstream>
#include <iostream>
#include <string>

using namespace std;

class User {
	public:
		User();
		User(string name, int acc_num, float balance, string status, string plan);
};

int main (int argc, char *argv[]) {

  if (argc == 0) { // for command line inputs only

  } else  if (argc == 0) { // argc should be 1 for correct execution with text inputs
    // argv[0] would be the first argument - <filename>

  } else {

  }
}